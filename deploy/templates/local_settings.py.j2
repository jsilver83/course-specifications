SECRET_KEY = '{{ secret_key }}'

DATABASES = {
    'default': {
        'ENGINE': '{{ database_engine }}',
        'NAME': '{{ database_name }}',
        'USER': '{{ database_user }}',
        'PASSWORD': '{{ database_password }}',
        'HOST': '{{ database_host }}',
        'PORT': '{{ database_port }}',
    }
}

DEBUG = {{ django_debug|string }}

ALLOWED_HOSTS = [
    '{{ apache_server_names|join("','") }}',
    '{{ ansible_default_ipv4.address }}',
]

STATIC_ROOT = '{{ django_project_static_dir }}'
MEDIA_ROOT = '{{ django_project_media_dir }}'

# A list of all the people who get code error notifications:
# https://docs.djangoproject.com/en/1.10/ref/settings/#std:setting-ADMINS
ADMINS = [
    {% for admin in admins %}
    ('{{ admin.name }}', '{{ admin.email }}'),
    {% endfor %}
]

EMAIL_HOST = '{{ email_host }}'
EMAIL_PORT = '587'
EMAIL_USE_TLS = True
EMAIL_HOST_USER = '{{ email_user }}'
EMAIL_HOST_PASSWORD = '{{ email_password }}'
SERVER_EMAIL = '{{ email_user }}'
DEFAULT_FROM_EMAIL = 'Course Specification <{{ email_user }}>'

BANNER_WEBSERVICE_BASE_URL = '{{ banner_webservice_base_url }}'
BANNER_WEBSERVICE_USERNAME = '{{ banner_webservice_username }}'
BANNER_WEBSERVICE_PASSWORD = '{{ banner_webservice_password }}'

STAFF_WEBSERVICE_BASE_URL = '{{ staff_webservice_base_url }}'
STAFF_WEBSERVICE_USERNAME = '{{ staff_webservice_username }}'
STAFF_WEBSERVICE_PASSWORD = '{{ staff_webservice_password }}'

CAMUNDA_BASE_URL = '{{ camunda_webservice_base_url }}'
CAMUNDA_USERNAME = '{{ camunda_webservice_username }}'
CAMUNDA_PASSWORD = '{{ camunda_webservice_password }}'
